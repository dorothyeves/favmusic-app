<% @songs.each do |song| %>
    <div class="container">
<div class="booyah-box col-10 offset-1">
<h1 class="col-10 offset-1"><%= song.name %></h1>
<p class="col-10 offset-1"><%= song.song %><span>|</span></p>
</div>
    </div>
<% end %>